# -*- mode: gitignore; -*-

# Compiled output: byte-code and native-compiled (Emacs 28+)
*.elc
*.eln
eln-cache/
package-quickstart.el

# Installed packages -- reproducible from init.el, do not version them
elpa/
straight/
straight-build/
quelpa/

# State and caches Emacs writes under user-emacs-directory
auto-save-list/
transient/
url/
tutorial/
server/
newsticker/
eshell/
recentf
places
projects
tramp
network-security.data
nsm.data
org-clock-save.el
bookmarks
bookmarks.bmk
.emacs.desktop
desktop/
semanticdb/

# Code generated by M-x customize.  init.el loads it, but it is machine
# written -- run `git add -f custom.el` if you want it tracked instead.
custom.el

# Auto-save and lock-file droppings
\#*\#
.\#*
*~

# macOS
.DS_Store
